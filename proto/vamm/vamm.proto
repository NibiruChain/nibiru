syntax="proto3";

package nibiru.vamm.v1;

option go_package = "github.com/NibiruChain/nibiru/x/vamm/types";

enum Direction {
  UNDEFINED = 0;
  ADD_TO_AMM = 1;
  REMOVE_FROM_AMM = 2;
}

message ReserveSnapshot {
 string token0_reserve = 1;
 string token1_reserve = 2;
 int64 timestamp = 3;
 int64 block_number = 4;
}

message Pool {
  string pair = 1;
  // ratio applied to reserves in order not to over trade
  string trade_limit_ratio = 2;
  string token0_reserve = 3;
  string token1_reserve = 4;
  string fluctuation_limit_ratio = 5;
}